import {cloneDeep} from 'lodash'
import {createStorage} from 'src/mixins/component-storage'
import {shallowMount} from '@vue/test-utils'

describe('component-storage', function test() {
  describe('createStorage', function test() {
    it('should create Component Storage', function test() {
      // todo need to write tests
      expect('foo').to.equal('foo')
    })
  })
})
